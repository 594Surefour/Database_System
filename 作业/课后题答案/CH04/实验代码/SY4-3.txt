题目3
（1）SELECT sno,sname FROM student WHERE sno IN (SELECT sno FROM sc WHERE grade>=90);
（2）SELECT cno,cname FROM course 
WHERE not EXISTS 
(SELECT * from sc where course.cno=sc.cno);
（3）（a）创建新表AvgGrade。
CREATE TABLE AvgGrade(sno int,Avg_Grade float);
     （b）将查询结果插入新表AvgGrade中。
INSERT INTO AvgGrade
SELECT sno,AVG(grade) FROM sc GROUP BY sno ;
     （c）查看AvgGrade表中记录。
SELECT * FROM AvgGrade;
（4）UPDATE sc SET grade=grade+5
WHERE 'MySQL数据库设计'=
(SELECT cname FROM course WHERE sc.cno=course.cno);
（5）DELETE FROM sc 
WHERE '计算机组成原理'=
(SELECT cname FROM course WHERE sc.cno=course.cno);
