题目1
（1）SELECT sno,sname FROM student;
（2）SELECT * FROM course WHERE cname='MySQL数据库设计';
（3）SELECT * FROM course WHERE cname LIKE '%数据库%';
（4）SELECT COUNT(*) FROM student;
（5）SELECT * FROM sc ORDER BY grade DESC;
题目2
（1）SELECT student.sno,sname,cname,grade FROM student INNER JOIN sc INNER JOIN course 
ON student.sno=sc.sno AND course.cno=sc.cno WHERE grade<60 and cname='MySQL数据库设计';
（2）SELECT student.sno,sname,cname,grade FROM student INNER JOIN sc INNER JOIN course 
ON student.sno=sc.sno AND course.cno=sc.cno WHERE sname='李四';
（3）SELECT student.sno,sname,count(cno),max(grade),min(grade),sum(grade),avg(grade)
FROM student left JOIN sc ON student.sno=sc.sno
GROUP BY student.sno;
（4）SELECT student.sno,sname,avg(grade)
FROM student INNER JOIN sc ON student.sno=sc.sno
GROUP BY student.sno
HAVING avg(grade)>70
ORDER BY avg(grade) DESC;
（5）SELECT student.sno,sname,cname,grade FROM student INNER JOIN sc INNER JOIN course 
ON student.sno=sc.sno AND course.cno=sc.cno WHERE cname='MySQL数据库设计';
题目3
（1）SELECT sno,sname FROM student WHERE sno IN (SELECT sno FROM sc WHERE grade>=90);
（2）SELECT cno,cname FROM course 
WHERE not EXISTS 
(SELECT * from sc where course.cno=sc.cno);
（3）（a）创建新表AvgGrade。
CREATE TABLE AvgGrade(sno int,Avg_Grade float);
     （b）将查询结果插入新表AvgGrade中。
INSERT INTO AvgGrade
SELECT sno,AVG(grade) FROM sc GROUP BY sno ;
     （c）查看AvgGrade表中记录。
SELECT * FROM AvgGrade;
（4）UPDATE sc SET grade=grade+5
WHERE 'MySQL数据库设计'=
(SELECT cname FROM course WHERE sc.cno=course.cno);
（5）DELETE FROM sc 
WHERE '计算机组成原理'=
(SELECT cname FROM course WHERE sc.cno=course.cno);
