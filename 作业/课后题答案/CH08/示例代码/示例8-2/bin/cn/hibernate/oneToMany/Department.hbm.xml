<?xml version="1.0"?>
<!DOCTYPE hibernate-mapping PUBLIC
      "-//Hibernate/Hibernate Mapping DTD 3.0//EN"
          "http://www.hibernate.org/dtd/hibernate-mapping-3.0.dtd">

<hibernate-mapping package="cn.hibernate.oneToMany">
<!-- 
class属性：实体类 
table属性：哪个表，可以省略，如果不写表名默认就是类的简单名称，一般都会写


-->
    <class name="Department" table="department">
        <id name="id">
            <generator class="native" />
        </id>
      <property name="name"></property>
       <!-- Employee属性，Set集合，表达的是本类与Employee的一对多的关系 
       class属性：代表关联的实体内容
       key：对方表中的外键列（多的那个表）
       inverse="false",默认为false，表示本方维护关联关系
       true本方不维护关联关系，表示由对方维护关联关系   ；但这个设置不会影响到一的一方获取多的一方的信息
       这是影响是否能设置外键的值（设成有效值或null值）对获取信息没有影响
       cascade：默认为none，代表不级联
       可设为delete，表示删除主对象时候，关联对象也做相同（删除）操作
       cascade="delete":级联删除，删除部门也跟着删除员工了
       也可设为：save-update，delete，all，none,多个关系时候可以使用逗号
       -->
       
       <set name="employee" inverse="false" cascade="delete">
       <key column="departmentId"></key>
       <one-to-many class="Employee"/>
       
       </set>
   
    </class>
    
   
 


</hibernate-mapping>
